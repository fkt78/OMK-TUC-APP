#!/bin/sh
# ソースコードが変更されたらバージョンを自動で 1 つ上げる
STAGED=$(git diff --cached --name-only)
if echo "$STAGED" | grep -qE '^src/|^index\.html$|^vite\.config\.|^public/' ; then
  node scripts/bump-version.mjs
  git add package.json
fi
